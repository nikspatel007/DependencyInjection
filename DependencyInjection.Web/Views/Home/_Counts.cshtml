@model System.Collections.Generic.List<DependencyInjection.Models.Numbers>


<div class="table-responsive">
    <table class="table table-hover">
        <thead>
            <tr>
                <th>#</th>
                <th>Result</th>
            </tr>
        </thead>
        <tbody>
        @foreach (var number in Model)
        {
            <tr>
                <td>@number.Value</td>
                <td>@number.Result</td>
            </tr>
        }
        </tbody>
    </table>
</div>